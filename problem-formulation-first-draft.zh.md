# 团队视觉情感感知：问题表述与初稿方案

**状态：** 内部短报告工作稿。  
**本稿范围：** 明确问题，并给出一套可在现有线上数据上**先试**的方案——不是最终的真人实验设计。

---

## 1. 问题表述

### 1.1 背景

设计、制造与协作式问题解决中的团队合作是动态的。成员的投入程度、积极性以及彼此之间的状态一致性，会随任务难度、时间压力和任务阶段而变化。问卷与可穿戴传感器难以在不打断团队的前提下连续使用。普通 RGB 视频是非接触、易部署的；预训练工具（Py-Feat、OpenFace、MediaPipe）已能提供面部动作单元（AU）、效价–唤醒（valence–arousal）、视线以及头/身体姿态。

挑战在于：**在人脸缺失、检测质量不均、任务阶段不断变化的条件下，如何从个体视觉线索走向随时间变化的团队层状态。**

### 1.2 我们所说的“状态”指什么

我们估计的是**外显视觉情感（outward visual affect）**，即由可观察的面部与姿态线索得到的**感知状态**，**绝不代表个体的真实情绪**。它也不是医学诊断、人格判断，或对个人绩效的评价。

个体输出可以是连续量（valence、arousal），和/或离散类别（积极 / 中性 / 消极）。团队输出应同时包含**水平**（群体看起来有多积极/多激活）与**结构**（齐心 vs 分化）。

### 1.3 核心问题

给定一段多人协作视频，在每个时间窗内我们观察到的是一组**多元、不完整**的信号：若干人 × 若干视觉特征 × 置信度/可见性标记。问题是：

1. 估计每位成员的视觉状态。  
2. 用比盲目平均更站得住脚的规则，把这些状态**融合**为团队状态。  
3. 按**问题解决的不同阶段**呈现团队状态（之后再跨实验条件呈现）。  
4. 检验融合后的序列是否**可用**：曲线足够稳定、可画图；对缺失数据的反应可控；日后能与主观评分或任务结果对照。

这是一个**系统与分析**问题（流水线 + 融合 + 时序呈现）。

### 1.4 约束（来自课程任务书）

- 仅使用 RGB 摄像头；不要求脑电、心电等。  
- 允许使用现有视觉模型；不必从头训练检测器。  
- 至少**两种融合方法**。  
- **不强制实时**。先录制 → 自动离线分析 → 交互展示即可。  
- 自采团队实验安排在**后期**。调试后端不必等志愿者实验。

### 1.5 本稿试图回答的问题

- Q1：预训练提取器能否在多人视频上给出每人的 valence/arousal（外加一种行为线索，如头部姿态）？  
- Q2：融合时应如何处理缺失或低置信度的人脸？  
- Q3：两种简单融合规则（不加权 vs 可靠性加权）是否会产生**不同且可解释**的团队时间线？  
- Q4：后端能否根据视频 + 时间戳输出分阶段摘要（团队状态均值 + 离散度）？

**本稿不做：** 主张视觉分数等于真实情绪；完成志愿者实验设计。

---

## 2. 初稿方案

### 2.1 策略：先在自有实验之前检验方法

工作分为两阶段。

| 阶段 | 数据 | 目标 |
|------|------|------|
| **阶段 A（当前）** | 公开 / 已采集的**线上**协作视频（如 AMI 风格的 3–4 人会议，或类似的群体情感 / 会议语料） | 打通**代码与后端**；比较融合方法；产出时间线与阶段图 |
| **阶段 B（后期）** | 自行设计的团队任务、两种条件、问卷 | 在我们可控的数据上验证同一套流水线 |

阶段 A 是**工程与方法检验**。阶段 B 是**实验验证**。先用线上数据，避免融合工作被伦理、排期和拍摄质量卡住。

### 2.2 流水线（第一版实现）

```text
线上 / 公开多人视频
        → 抽帧（例如 1 fps）
        → 分配成员 ID（固定座位/ROI；仅在必要时做跟踪）
        → Py-Feat（或同类工具）：valence、arousal、头部姿态，
           以及可见性 / 置信度
        → 个体状态时间序列
        → 融合 A：有效人脸的简单平均
        → 融合 B：可靠性加权平均
        → 团队水平 + 离散度（+ 可选短时平滑）
        → 阶段聚合（若存在片段时间）
        → CSV + 图表 + 简易查看器（上传视频 → 时间线）
```

**特征集（最低配置）：** valence/arousal 作为情感通道；头部姿态作为行为通道。其他线索（AU、视线）可以保存，但第一轮融合测试不必使用。

**ID：** 对就座会议，按画面区域映射到人。不新训检测/跟踪模型。

**运行方式：** 先做离线批处理。一层薄 UI（如 Gradio/Streamlit）即可表明后端是一套系统，而不是笔记本脚本堆砌。

### 2.3 拟在线上数据上测试的融合方法

记第 \(i\) 人在时间窗 \(t\) 的 valence 为 \(v_{i,t}\)，置信度为 \(c_{i,t}\)，可见性为 \(vis_{i,t}\in\{0,1\}\)。

**方法 A — 基线（有效人脸平均）**  
仅对有效人脸的 \(v_{i,t}\) 取平均。若无人有效，将该窗标为**缺失**（不要填 0）。

**方法 B — 可靠性加权平均（建议作为默认）**

$$
w_{i,t}=c_{i,t}\cdot vis_{i,t},\qquad
\hat{v}_t=\frac{\sum_i w_{i,t}v_{i,t}}{\sum_i w_{i,t}}
$$

arousal 用同一规则。可选：再乘以该人近期有效帧比例。

**团队结构（始终计算，不是第三个“黑箱”）：** 加权标准差 \(s_t\) 作为**离散度**（分化 vs 齐心）。可选后续：\(\Delta v\) 的成对同步性。

**平滑：** 仅用于展示的短时滑动平均（如 15–30 秒）；统计仍用未平滑序列。

**阶段呈现：** 若线上语料带有会议阶段，或我们按时间切分（开场 / 讨论 / 收尾），则对每个阶段报告：\(\hat{v}\) 均值、\(s\) 均值、数据缺失率。这是对“如何在不同阶段呈现群体状态”的第一版回答。

### 2.4 计划使用的线上数据

群体情感识别已是标准问题。我们使用现有语料，用于：（i）把真实的 3–4 人协作视频送入流水线；（ii）在遮挡与人脸质量不均的条件下压力测试融合；（iii）若存在标签，做一次健全性检查：团队层曲线是否随独立分数变动。

**相关公开资源（背景）。**  
**Group Affect Database 2.0** 是经典的图像级群体情感基准，将群体标为积极 / 中性 / 消极，是该文献的常用参照。2025 年已有工作在该基准上融合人脸、人体姿态与场景。**GAViD** 是较新的**视频级**群体情感数据集（量级为数千片段），带三元 valence/emotion 标签及基线。它们说明 **个体 → 群体** 情感是公认任务。但作为我们的**主开发集**不够理想：许多条目仍是图片或短片段，而不是角色稳定、共享同一问题解决过程的长会话。

**主开发数据：AMI Meeting Corpus。**  
我们计划在 **AMI** 上开发与调试。它比合影类基准更贴近课程设定：

- **团队规模：** 一般为 **四人**，落在任务书要求的 3–4 人范围内。  
- **共同任务：** 参与者以设计团队身份协作设计**遥控器**。  
- **角色：** 项目经理、市场专家、UI 设计师、工业设计师。  
- **座位：** 人员大多**座位固定**，成员 ID 可用**座位 / 画面区域映射**（左上 → P1 等），任务书明确允许。阶段 A 可以不做 YOLO + 跟踪。

我们**不会**处理全部约 100 小时。一小段切片即足以证明后端：大约 **3–5 场会议**，或 **20–40 个短片段**，约按 **1 帧/秒** 采样。一场 20 分钟会议大约是 1200 帧 × 4 人——足以做缺脸统计和融合对比，又小到可以离线跑 Py-Feat。

**在 AMI 上使用 Py-Feat。**  
检测类模型可一次输出表情、valence、arousal、头部姿态、视线、AU 与关键点。第一稿只保留 **valence / arousal + 头部姿态**，写成表：`time | person | valence | arousal | pitch | yaw | confidence | visible`，再按时间 `groupby` 做融合。

**可选对齐标签：AMI-ME。**  
若在自有问卷之前，希望有一个外部数值与融合后的团队状态对照，**AMI-ME**（2026 年基于 AMI 的基准）将会议切成时间片段，并由多名标注者给出 **会议有效性** 分数（1–5）。这不是“真实情绪”，但是独立的**片段级**目标。第一次检查可以是：高效 vs 低效片段在团队 valence/arousal 均值或**成员离散度**上是否不同？Spearman 相关或两组检验即可。这是对流水线的**健全性检查**，不是最终验证结论。

---

## 3. 开题汇报分工（四人，按提纲）

开题 PPT 按学院提纲五块组织（首页含题目、四名成员、指导老师、日期；正文不超过约 10 页）。人名未定时用 **成员 A–D**；确定后直接替换。每人主讲一块，第五块「进度计划」由全组共同填表、由 **成员 A** 串讲。

| 提纲 | 主责 | 开题要交的内容 | 课题里对应的长期职责 |
|------|------|----------------|----------------------|
| **1. 课题背景** | **成员 A** | 人机协作 / 团队状态为何要连续、非侵入感知；问卷与生理传感器的局限；RGB + 预训练视觉工具的定位；伦理边界（外显感知 ≠ 真实情绪） | 问题表述与报告统稿；知情同意与隐私口径 |
| **2. 研究内容** | **成员 B** | 核心问题四条：个体估计 → 融合 → 分阶段呈现 → 可用性检验；输入输出与边界（3–4 人、RGB、不训检测器）；Q1–Q4 | 阶段划分与「团队状态如何呈现」；后期实验条件设计 |
| **3. 文献调研** | **成员 C** | Group Affect / GAF 2.0、视频级群体情感（如 GAViD）、AMI 会议语料、Py-Feat/OpenFace/MediaPipe；说明为何主用 AMI 而非合影基准 | AMI（及可选 AMI-ME）下载、切片、座位 ROI、抽帧与元数据 |
| **4. 实施方案** | **成员 D** | 技术路线图：抽帧 → ID → Py-Feat → 融合 A/B → 离散度 → 时间线/阶段图 → 简易界面；公式与缺脸规则 | 融合算法、后端接口、图表与 Gradio/Streamlit |
| **5. 进度计划** | **全组 / A 串讲** | 本页的分工表 + 周次计划 | A 盯节点；每人按表交差 |

**交叉约定（避免四人各干各的）：**

- C 产出的 `time | person | valence | … | confidence | visible` 表是 D 融合的唯一输入格式。  
- B 规定时间窗（如 1 s）和阶段切法（开场 / 讨论 / 收尾，或 AMI-ME 片段），A/D 画图必须用同一套切法。  
- A 负责把「感知状态 ≠ 真实情绪」写进每一版对外材料。  
- 开题后，C 与 D 是工程主路径；B 转向实验设计；A 转向问卷、伦理与终稿。

---

## 4. 进度计划（粗纲）

先 **阶段 A（公开线上数据，打通后端与融合）**，再 **阶段 B（自有实验）**。周次可按学期日历整体平移；与开题模板一样具体到人。

| 时间节点 | 工作任务 | 完成人 |
|----------|----------|--------|
| **第 1–2 周** | 文献调研（群体情感、AMI、预训练视觉工具）；冻结问题表述与伦理口径；确定抽帧率、特征集（valence/arousal + 头部姿态）、两种融合规则。开题 PPT：A 背景、B 研究内容、C 文献、D 实施方案、全组进度表。 | A（背景/统稿）、B（研究内容）、C（文献+数据调研）、D（方案图/公式） |
| **第 3 周** | 下载 AMI 切片（约 3–5 场或 20–40 段）；座位/ROI → 四人 ID；环境与 Py-Feat 跑通单人短视频。 | C（主）、D（环境/接口）、B（ID 规则） |
| **第 4–5 周** | 全员特征表导出；实现融合 A（有效人脸平均）与融合 B（可靠性加权）；缺脸不填 0；输出团队水平 + 离散度时间线。 | D（融合主）、C（特征质量/缺失率）、A（曲线是否可解释） |
| **第 6–8 周** | 按阶段/片段汇总 \(\hat{v}\)、\(s\)、缺失率；可选 AMI-ME 有效性做 Spearman / 两组比较（健全性检查）；离线流水线 + 简易查看器（上传/选视频 → 时间线）。阶段 A 里程碑：后端在公开数据上可演示。 | D（系统）、C（数据对齐）、B（阶段呈现）、A（检查表述与图注） |
| **第 9–10 周** | **阶段 B 设计（本稿不展开细节）：** 3–4 人协作任务、至少两种条件、短问卷与任务指标、知情同意；机位与阶段日志模板。 freeze 阶段 A 的采样率、ROI 方案、默认融合。 | B（实验主设计）、A（问卷/伦理）、C/D（采集清单与复用同一后端） |
| **第 11–13 周** | 小规模录制与问卷；同一流水线出个体/团队曲线；融合 A vs B；视觉估计 vs 问卷 / 任务成绩 / 条件对比。 | 全组：B 主持实验，C 采集与质检，D 出分析图，A 对照问卷与结论边界 |
| **第 14–16 周** | 系统联调与演示视频；工程指标（检出率、缺失率、速度）；课程报告与答辩材料。 | 全组：D 演示系统，A 报告统稿，B 实验章节，C 数据与文献附录 |

**各人一条主线（便于填「完成人」栏）：**

- **成员 A：** 背景意义、伦理表述、开题统稿、后期问卷与报告终稿。  
- **成员 B：** 研究内容与边界、阶段如何切、后期实验方案。  
- **成员 C：** 文献与公开数据、AMI 处理、Py-Feat 特征落地。  
- **成员 D：** 融合方法、时间线/界面、系统演示。
